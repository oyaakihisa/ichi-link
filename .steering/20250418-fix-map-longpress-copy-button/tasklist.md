# タスクリスト

## 🚨 タスク完全完了の原則

**このファイルの全タスクが完了するまで作業を継続すること**

### 必須ルール
- **全てのタスクを`[x]`にすること**
- 「時間の都合により別タスクとして実施予定」は禁止
- 「実装が複雑すぎるため後回し」は禁止
- 未完了タスク（`[ ]`）を残したまま作業を終了しない

### 実装可能なタスクのみを計画
- 計画段階で「実装可能なタスク」のみをリストアップ
- 「将来やるかもしれないタスク」は含めない
- 「検討中のタスク」は含めない

### タスクスキップが許可される唯一のケース
以下の技術的理由に該当する場合のみスキップ可能:
- 実装方針の変更により、機能自体が不要になった
- アーキテクチャ変更により、別の実装方法に置き換わった
- 依存関係の変更により、タスクが実行不可能になった

スキップ時は必ず理由を明記:
```markdown
- [x] ~~タスク名~~（実装方針変更により不要: 具体的な技術的理由）
```

### タスクが大きすぎる場合
- タスクを小さなサブタスクに分割
- 分割したサブタスクをこのファイルに追加
- サブタスクを1つずつ完了させる

---

## フェーズ1: UI修正

- [x] SlidePanel.tsxの長押しモード・変換結果モードで使用される各行のCopyButtonを削除
  - [x] 住所行のCopyButton削除（278行目付近）
  - [x] WGS84座標行のCopyButton削除（291行目付近）
  - [x] Tokyo Datum座標行のCopyButton削除（301行目付近）
  - [x] 長押しモードのGoogle Maps行のCopyButton削除（365行目付近）

- [x] 「全部コピー」ボタンの表記を「コピー」に変更（269行目付近）

- [x] CopyButtonインポートの確認と整理
  - [x] CopyButtonがPOIモードのGoogle Maps行でまだ使用されているか確認
  - [x] ~~使用されていない場合はインポート文を削除~~（POIモードで使用されているため削除不要）

## フェーズ2: 品質チェックと修正

- [x] すべてのテストが通ることを確認
  - [x] `npm test` - 97 tests passed
- [x] リントエラーがないことを確認
  - [x] `npm run lint` - 0 errors（既存のwarningのみ）
- [x] 型エラーがないことを確認
  - [x] `npm run typecheck` - パス

---

## 実装後の振り返り

### 実装完了日
2025-04-18

### 計画と実績の差分

**計画と異なった点**:
- 計画通りに実装完了。特に差分なし。

**新たに必要になったタスク**:
- なし

**技術的理由でスキップしたタスク**:
- CopyButtonインポート削除: POIモードのGoogle Maps行で使用されているため削除不要

### 問題の原因分析

- 地図長押し時のスライドパネルで、各情報行に個別のコピーボタンが配置されていた
- 「全部コピー」ボタンがあるため、個別コピーボタンは冗長であった

### 修正内容のサマリー

1. `SlidePanel.tsx`の以下の箇所からCopyButtonを削除:
   - 住所行
   - WGS84座標行
   - Tokyo Datum座標行
   - Google Maps URL行（長押しモード）
2. 「全部コピー」ボタンの表記を「コピー」に変更
3. POIモードのGoogle Maps行のCopyButtonは維持（スコープ外）

### 学んだこと

**技術的な学び**:
- SlidePanelコンポーネントは3つのモード（pin/conversion/poi）を持ち、モードごとにUI表示が異なる
- CopyButtonコンポーネントは複数の場所で再利用されているため、削除前に使用箇所の確認が必要

**プロセス上の改善点**:
- スコープを明確に定義したことで、POIモードへの不要な変更を回避できた

### 次回への改善提案
- 変換結果モードとの表示一貫性を検討する価値がある（変換結果モードでは個別CopyButtonが表示される）
- wgs84CoordとtokyoCoordをuseMemoでラップすることでパフォーマンス最適化が可能（既存のLint警告を解消）
